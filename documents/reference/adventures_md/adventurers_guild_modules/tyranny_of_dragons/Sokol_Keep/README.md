# Secrets of Sokol Keep — RAG Pack

This is a retrieval-optimized Markdown export of **DDEX1-2 Secrets of Sokol Keep**.  
Use file names and the `id` fields for chunk IDs. Each document has:
- concise summary
- canonical `id` and `aliases`
- `tags` for filtering
- cross-references with relative links
- short Q&A for retrieval augmentation

Suggested embedding chunk size: ~800–1200 tokens; files are already broken into small, topical units. 

## Structure
- `index.md` – master metadata and synopsis
- `scenes/` – scene-by-scene beats
- `locations/` – locations with keyed rooms and navigation
- `npcs/` – major NPCs with motives, secrets, and voice
- `mechanics/` – tables, rules notes, and scaling
- `handouts/` – player-facing texts and clues
- `items/` – notable objects and treasure

> Licensing: Personal-use reference derived from your legally obtained copy. Do not redistribute.
